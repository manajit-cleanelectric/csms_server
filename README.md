# OCPP-RPC Express Server

A complete implementation of an OCPP (Open Charge Point Protocol) server using OCPP-RPC and Express.js.

## Quick Start

1. Install dependencies: `npm install`
2. Start server: `npm start`
